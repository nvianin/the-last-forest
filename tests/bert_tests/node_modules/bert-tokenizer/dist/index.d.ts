import WordPieceTokenizer from './wordpiecetokenizer';
export declare class BertTokenizer {
    private doLowerCase;
    tokenizer: WordPieceTokenizer;
    clsId: number;
    sepId: number;
    maxSeqLength: number;
    constructor(pathToVocabulary?: string, doLowerCase?: boolean, maxSeqLength?: number);
    tokenize(text: string): number[];
    convertIdsToTokens(ids: number[]): string[];
    convertTokensToId(token: string): number[];
    convertSingleExample(text: string): {
        inputIds: number[];
        segmentIds: number[];
        inputMask: number[];
    };
}
